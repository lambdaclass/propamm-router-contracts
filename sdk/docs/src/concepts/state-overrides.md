# State overrides

> TODO: how the pAMM venues price off-chain liquidity, why plain `eth_call`
> quotes see stale prices, the two Titan sources (WebSocket stream and
> `titan_getPammStateOverrides`), and how the SDKs attach the overrides plus
> block number/timestamp to quote simulations.
